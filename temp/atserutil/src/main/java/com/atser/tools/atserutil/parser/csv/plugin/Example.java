/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package com.atser.tools.atserutil.parser.csv.plugin;

/**
 *
 * @author droldan
 */
public class Example {
    /*
    
    File fcsv = new File("C:\\Users\\droldan\\Documents\\02.GanttExcelTemplates\\T02.TplSchedule_SoftwareProjectsTask.csv");
        try {
            CsvContext ctx = new CsvContext(fcsv);
            Iterable<CSVRecord> records = ctx.getpCsvDoc().getRecords(CSVFormat.DEFAULT.withHeader(new String[]{"ID", "Parent", "Description"}));
            for (CSVRecord record : records) {
                String code = record.get("ID");
                String firstName = record.get("Description");
                System.out.println(code + " - " + firstName);
            }
        } catch (FormatDocumentNotAllowedException | NotFoundMimeTypeException | IOException ex) {
            Logger.getLogger(MainF.class.getName()).log(Level.SEVERE, null, ex);
        }
    
    */
}
